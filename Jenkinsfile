@Library('helssy-shared-libraries@master') _
//The underscore (_) is not a typo! You need this underscore if the line immediately after the @Library annotation is not an import statement.
// _ permet d'importer le script qui se situe dans /vars
   
    helssyFrontendPracticienPipeline {
    servicename = 'mf_practician'
}